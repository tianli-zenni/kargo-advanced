apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: kargo-advanced
  namespace: argocd
spec:
  generators:
  - git:
      repoURL: https://github.com/tianli-zenni/kargo-advanced.git
      revision: HEAD
      directories:
      - path: env/*
  template:
    metadata:
      name: kargo-advanced-{{path.basename}}
      annotations:
        kargo.akuity.io/authorized-stage: kargo-advanced:{{path.basename}}
    spec:
      destination:
        namespace: kargo-advanced-{{path.basename}}
        # Specify either a cluster destination name or server address:
        name: in-cluster
        # server: https://REPLACEME
      project: kargo-advanced
      source:
        path: ./
        repoURL: https://github.com/tianli-zenni/kargo-advanced.git
        targetRevision: env/{{path.basename}}
